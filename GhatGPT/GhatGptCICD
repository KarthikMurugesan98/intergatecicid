name: UiPath Workflow
jobs:
   build:
    runs-on: windows-latest
    steps
    -  name: Checkout Repository
       uses: actions/checkout@v2

    - name: Setup UiPath Workflow
      run:
        uipath execute --workflow-file
        "Main.Xaml"

     
